# Node Backend Configuration

# Server Port
PORT=5000

# WanderAI LLM Backend (FastAPI service)
# WanderAI runs on port 8000 and provides modular LLM-based travel planning
# Endpoints: POST /v1/chat, GET /v1/health, POST /v1/webhook
WANDERAI_BACKEND_URL=http://localhost:8000
# Environment
NODE_ENV=development
